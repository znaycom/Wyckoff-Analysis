# Changelog

## v2.1.0 (2026-04-02)

**量化引擎深度修复 + 持仓健康诊断 + 全系统 Bug 清理**

本次更新是一次系统级代码审查驱动的大修，修复了多个影响选股正确性和回测可靠性的核心 Bug，并新增了持仓健康诊断能力。

### 核心引擎修复
- **修复 markup 阶段检测永远失效**：`_detect_markup_entry` 中 `isna().any()` 对 rolling 序列永远为 True，导致主升通道的阶段识别和退出信号从未生效。
- **修复 Step3 供需摘要全部为零**：`_build_supply_demand_summary` 因 Pandas 索引对齐问题，所有量价统计（放量下跌、缩量回踩、低量测试）注入给 LLM 的数据全是 NaN。
- **修复 RPS 仅在 L1 子集排名**：RPS 排名现在基于全市场 universe（L1 输入前的 4000+ 股票池），避免在 800 只 L1 子集中排名导致阈值系统性偏宽。
- **修复 Step3 漏传 4 个关键参数**：`generate_stock_payload` 调用点补传 `track`、`exit_signal`、`exit_price`、`exit_reason`，LLM 现在能看到退出预警和轨道上下文。

### 回测修复
- **修复 regime 分层统计全错**：回测中 `_calc_market_breadth` 返回值不含 `regime` 键，导致所有交易都被标为 NEUTRAL。现在正确捕获 `_analyze_benchmark_and_tune_cfg` 的返回值。
- **修复停牌股持有期算短**：`_open_on_or_after` 返回的 `actual_entry_date` 不再被丢弃，持有窗口和退出锚点从实际成交日起算，停牌 3 天复牌的股票不再系统性失真。
- **修复 Sharpe/Calmar 比率虚高**：年化频率从固定 250 次/年改为 `250 / hold_days`，hold_days=30 时 periods_per_year=8.33，数值不再被人为放大 30 倍。

### 新增：持仓健康诊断
- **新增 `core/holding_diagnostic.py`**：复用引擎的 L2 通道分类、L4 触发检测、L5 退出信号等能力，对任意持仓做结构化健康诊断（均线结构、Wyckoff 定位、止损距离、量能指标、综合评级）。
- **Step4 自动注入诊断**：`_process_one_position` 现在会调用 `diagnose_one_stock()` 并将诊断结果注入 LLM 上下文，AI 决策时可以看到每只持仓的完整健康状态。
- **新增 CLI 工具 `scripts/diagnose_holdings.py`**：支持 `--codes/--costs` 快速诊断和 `--from-portfolio` 从 Supabase 读取实盘持仓，输出 text/markdown/json 三种格式。
- **修复多标签通道误判**：`_classify_track` 从精确匹配改为子串匹配，`"主升通道+点火破局"` 正确归入 Trend 轨道。
- **修复单股诊断 RPS=100**：诊断模式下关闭 RPS 过滤（单股 universe 排名无意义），只用均线/RS/量能等绝对指标做通道判断。

### 风控与运维
- **修复 VIX 轮询无限循环**：`_fetch_vix_until_ready` 新增最大重试次数（默认 12 次），超限降级返回而非永远挂起 CI。
- **修复 Step4 线程异常崩溃**：`future.result()` 加 `try/except`，单只股票数据异常不再终止整个 Step4 流程。
- **修复 `datetime.utcnow()` 废弃警告**：全量替换为 `datetime.now(timezone.utc)`，覆盖 6 个文件，兼容 Python 3.12+。

### 参数调优（A/B 回测验证）
- **LPS 缩量阈值**：`lps_vol_dry_ratio` 0.55 → 0.48（Sharpe 0.831 → 2.325）
- **止损/止盈**：SL -6% → -7%，TP 15% → 18%（盈亏比 2.57:1，Sharpe 最优点）
- **持有周期**：15 → 30 交易日（Sharpe 单调递增，Wyckoff 信号需要更长兑现期）

## v2.0.3 (2026-03-28)

**窗口口径统一到 320 交易日，文案与默认参数全面对齐**

- **交易日窗口全链路统一**：漏斗筛选、批量研报、再平衡、回测与导出入口的默认回看窗口统一为 `320` 个交易日，避免不同模块口径不一致。
- **单股分析更稳**：单股大师模式默认改为近 `320` 个交易日输入，兼顾 MA200 计算稳定性与响应速度。
- **超时策略放宽**：单股 LLM 默认超时调整为 `REQUEST=90s / TOTAL=240s`，降低复杂输入下的误判超时。
- **文档与页面说明同步**：README、策略手册与页面相关文案统一更新为最新口径，减少“代码已改但说明仍旧”的理解偏差。
- **清理临时排查痕迹**：移除临时诊断日志输出，保留用户可见错误提示，减少线上噪音。

## v2.0.2 (2026-03-17)

**多厂商 LLM、多端通知与推荐体验优化**

- **多厂商大模型**：设置页与 LLM 调用层支持 Gemini / OpenAI / 智谱 / Minimax / DeepSeek / Qwen，可配置各厂商 API Key 与默认模型；AI 分析单股与 Step3 批量研报均可按所选厂商调用。
- **多端通知**：除飞书外，支持企业微信、钉钉 Webhook；设置页可分别配置三端地址，单股研报、批量下载完成、Step3 研报及预演均会按配置同时推送至飞书/企微/钉钉。
- **定时任务多厂商与多端**：GitHub Secrets 可配置 `DEFAULT_LLM_PROVIDER`、`WECOM_WEBHOOK_URL`、`DINGTALK_WEBHOOK_URL`；Step3 支持传入 provider 与三端 webhook，实现后台研报多厂商与多端推送。
- **推荐表价格同步复用日线**：定时任务中推荐表价格与涨跌幅更新改为复用 Step2 已拉取的日线最后一笔收盘价，不再重复请求全市场行情，避免超时与重复拉取。
- **推荐跟踪页默认排序**：列表默认按「涨幅从高到低 → 推荐次数从高到低 → AI 推荐优先 → 推荐日期从新到旧 → 分值从高到低 → 现价从高到低」多级排序，并保留按单列排序的切换选项。
- **代码与注释**：移除“新增”“供复用”等中间环节类注释，精简说明文案。

## v2.0.1 (2026-03-14)

**新增推荐跟踪：连接筛选历史与实盘表现**

- **新增「推荐跟踪」页面**：记录每日定时任务生成的威科夫推荐股票，直观展示推荐后的累计涨跌幅，实现“选股-跟踪-复盘”的闭环。
- **数据库持久化**：采用 Supabase 存储推荐历史，包括代码、名称、推荐原因、加入价及推荐日期等核心字段。
- **全自动价格同步**：定时任务流水线新增价格同步环节，每日自动刷新全表股票的最新收盘价并重算表现（涨红跌绿可视化）。
- **高性能索引优化**：数据库底层使用 `INT` 型存储代码与日期（YYYYMMDD），配合 B-Tree 索引，确保在万级历史数据下检索与排序依然秒开。
- **智能搜索与排序**：支持根据股票代码/名称实时搜索，支持按涨跌幅和推荐日期快捷排序；展示层自动处理 A 股代码前导零补全。
**候选更克制，减少追高，AI 更重视左侧机会**
- **主升信号更克制**：进一步收紧右侧强势股的入池条件，优先过滤已经远离年线、容易进入末端加速的高位标的，减少“看起来很强、但追进去就难受”的情况。
- **SOS/LPS 质量门槛抬高**：对点火与缩量回踩的质量要求更高，尽量把 AI 和实盘注意力集中到更干净、更像样的结构上。
- **AI 候选分配更偏左侧**：在总容量不变的前提下，把更多名额留给潜伏、吸筹、地量与护盘类候选，降低单边右侧抱团，让策略更贴近“先看胜率，再看热度”的思路。
- **文案同步更新**：产品说明与策略手册已同步这次收紧口径，避免页面、策略文档与实际筛选逻辑脱节。

## v2.0.0 (2026-03-11)

**架构升级：重计算迁出 Streamlit，Web 端进入前后台分离阶段**

- **后台化 Wyckoff Funnel**：`Wyckoff Funnel` 页面不再在 Streamlit Community Cloud 里本地拉全量 OHLCV 和执行漏斗，而是改为前台提交参数、GitHub Actions 后台执行、页面只读轻量结果。
- **后台化批量 AI 研报**：`AI 分析` 页保留单股本地模式；批量代码分析与漏斗候选分析统一改成后台任务，避免页面进程长时间持有大批量 OHLCV 和大模型上下文。
- **新增 Web 后台任务工作流**：加入 `web_quant_jobs.yml`、GitHub Actions 触发适配层与结果 artifact 协议，无需额外服务器即可支撑页面内的后台计算。
- **结果承载改为轻量 artifact**：后台任务统一输出结构化 JSON artifact，页面通过 GitHub API 轮询状态并读取结果，不额外引入新的 Supabase 结果表。
- **内存与稳定性优化**：Streamlit 端只保留参数输入、运行状态和摘要展示，显著降低 Community Cloud 的内存峰值与超时概率。
- **文档全面更新**：README、策略手册、页面文案和目录说明统一切换到“前台触发后台任务”的新口径。

## v1.9.1 (2026-03-10)

**核心量化逻辑 Bug 修复**

- **修复 RPS 斜率判定机制**：修正了以单日收益率进行斜率测算，导致长周期匀速上涨优质标的被错杀的问题。已更正为提取累计涨幅曲线坡度（Cumulative Return Slope）。
- **修复 SOS 下游分发缺失**：移除了 Layer 4 测试中过早的“点火破局”去重阻断，修复了该信号未进入终态 `results['sos']` 导致下游环节丢失优先级和标志的问题。
- **修正三处具有前视偏差（Lookahead Bias）或量化溢出的指标算法**：
  - Spring（终极震仓）判定支撑区间时，完全剔除正在下探的 `T-1` 日K线干扰。
  - LPS（最后支撑点）最大量参考窗口彻底向后平移，不再混杂当前核审区间数据。
  - SOS均线上穿动作的对照系，修正为 `昨日收盘价 < 昨日MA50` 且 `今日收盘价 > 今日MA50`，不再错配。

## v1.9.0 (2026-03-08)

**AI 决策表达升级，盘前风险提示更清晰**

- **AI 研报升级为三阵营**：研报从原先较粗的二分类，升级为 `逻辑破产 / 储备营地 / 处于起跳板` 三阵营表达，阅读上更接近真实交易中的“淘汰 / 等待 / 可执行”节奏。
- **持仓管理更贴近实盘**：持仓页新增 AI 建议对照视图，用户可以更直观地看到当前持仓与最新建议是否一致，并及时识别过时建议。
- **市场信号栏上线**：登录后的页面顶部新增“最新交易日市场信号栏”，统一提示大盘水温、A50（盘前风向标）与 VIX（恐慌指数），帮助用户先看环境再看个股。
- **盘前风险分级更细**：盘前风险不再只有极端预警，新增 `CAUTION` 过渡档位，用于提示“情绪已受扰动，但还未进入极端风险”，更方便控制节奏和仓位。
- **展示口径更易懂**：面向用户的页面文案统一切换为中文表达，例如“大盘偏强 / 中性 / 偏弱”，并改用 A 股更熟悉的红涨绿跌颜色习惯。
- **选股覆盖范围扩大**：初筛市值门槛进一步下调，更有利于把一部分高弹性中小盘机会纳入观察范围。

## v1.8.0 (2026-03-04)

**盘前风控：引入大盘情绪防御网 & 算法降噪优选**

- **盘前风控**: 新增 `scripts/premarket_risk_job.py` 补充盘前监控，当前调度为每日 07:55 (Asia/Shanghai) 自动化预警大盘风险。
- **情绪监测**: 结合富时 A50 期指盘前走势与 VIX 恐慌指数 (多数据源多级降级保障)，研判市场是否处于 `RISK_OFF` 或 `BLACK_SWAN` (黑天鹅) 状态。
- **飞书通知**: 盘前自动定向发送飞书战报，通过“防御性设计”帮助避免在情绪崩溃日盲目开仓。
- **算法降噪优选 (网格回测指引)**:
  - **默认关闭 EVR 触发器**: 基于全市场最新网格回测数据（胜率与赔率分布偏态问题），判定高位“放量滞涨”信号假阳性过高，将其默认降级关闭，专注高质量的 LPS（缩量回踩）与 Spring（终极震仓）。
  - **缩减 TopN 出票数**: 回测脚本每日取样容量自 Top 6 收紧至 Top 3，切断低质量“凑数”信号对长线收益中位数的拖累。

## v1.7.1 (2026-03-02)

**低成本数据增强：广度 + RPS + 轻量回测**

- **Layer2 增强**：新增 `RPS50 / RPS120` 截面强度过滤（与原 RS 并行生效），优先保留全市场相对更强的标的
- **水温增强**：大盘总闸加入“市场广度（站上 MA20 占比）”，在冰点期强制收紧阈值，在扩散期允许适度放宽
- **动态阈值联动**：水温调参新增 RPS 阈值联动（`rps_fast_min / rps_slow_min`），避免单一条件误判
- **可观测性**：漏斗与 Step3 上下文新增广度字段，日志与研报上下文可直接复盘当日水温
- **工具新增**：增加 `scripts/backtest_runner.py`，支持日线级轻量回测并输出 `summary_*.md` 和 `trades_*.csv`

## v1.7.0 (2026-03-01)

**选股更稳准、风控更聪明、推送更可靠**

- **停牌识别**：自动跳过当日数据缺失（停牌/数据延迟）的股票，不再把停牌前的旧行情误判为有效信号，避免买入复牌后连续跌停的股票
- **截面日期对齐**：个股数据与大盘数据强制对齐到同一交易日，相对强弱计算结果更准确，减少因时间轴错位产生的虚假信号
- **震仓背景校验**：Spring（终极震仓）必须发生在横盘震荡区间内，单边下跌途中的反弹不再被误认为买点
- **EVR 次日确认**：巨量滞涨信号默认等到次日验证低点未被跌破后才上报，过滤掉游资一日游和拉高出货的干扰
- **决策降级保护**：AI 给出非法止损价时，持仓单自动降级为「持有」而非拒单，不会因模型幻觉导致持仓无人看管
- **Telegram 推送支持完整格式**：推送内容加粗、代码块、链接等格式现在均可正确显示，不再出现格式符号外漏

## v1.6.0 (2026-02-27)

**稳定性与体验优化**

- 数据拉取失败时会明确展示失败的免费数据源，避免误会是程序故障
- 首页拉取在“确定数据源全失败”时不再反复重试，避免长时间卡在加载中
- 自定义导出「A股个股历史（日线）」支持多数据源自动降级，提升成功率
- 注册成功提示文案优化：无需邮件确认即可登录

## v1.5.0 (2026-02-27)

**私人决断：你的账户，单独一份买卖建议**

- 选股和研报跑完后，若你配置了持仓和 Telegram，会单独发一份「针对你账户的 Buy/Hold/Sell 建议」到你的 Telegram，只有你能看到
- 不配则自动跳过，不影响前面的选股和研报

## v1.4.0 (2026-02-27)
- 📊 **Feature**: Wyckoff Funnel 从全市场（主板 + 创业板）筛选，输出**最值得次日操作的 6 只股票**，含观察池与操作池分层。
- 🌡️ **Feature**: 根据大盘水温动态调整筛选力度，弱势市场自动提高门槛。
- 📨 **Notification**: 飞书通知支持长文自动分片发送，避免消息截断。
- 🧾 **Observability**: 模型输入落盘与日志摘要，便于核查筛选上下文。
- ⚙️ **Automation**: GitHub Actions 定时任务，支持工作日自动执行与手动触发。

## v1.3.0 (2026-02-09)
- 🤖 **Feature**: 新增「AI 分析」页面，基于多维度投委会逻辑生成深度研报（技术结构、价值质地、情绪题材、风险与交易计划等）。
- 📌 **Feature**: 支持两种分析来源——**指定股票代码**（最多 6 只）或**先筛选再分析**（在本页完成筛选，无需跳转沙里淘金页）。
- 🧭 **Feature**: AI 分析页内嵌筛选：选择战术（抗跌主力 / 突破临界 / 异常吸筹·出货 / 启动龙头）+ 股票池（手动输入或板块），执行后直接取前 6 只用于研报。
- ⏳ **UX**: 拉取数据与生成研报等待期间，随机展示股市名人名言，缓解等待感。
- 📝 **Copy**: 页面说明与筛选区块文案优化，更友好、适合直接面向用户展示。
- ✅ **体验**: AI 分析页与沙里淘金的筛选结果一致性提升，避免同一策略在不同页面表现不一致。

## v1.2.1 (2026-01-19)
- 🔐 **Auth**: 引入 **Supabase Auth** 登录系统（邮箱/密码），保护用户个性化数据。
- ⚙️ **Feature**: 新增「设置」页面，支持配置飞书 Webhook 和 Gemini API Key。
- ☁️ **Sync**: 实现用户配置（Key/Webhook）**云端自动同步**，支持多端漫游。
- 🧭 **UI**: 导航栏新增「设置」入口，配置更方便。
- 🧹 **Clean**: 移除首页冗余配置项，界面更清爽。

## v1.1.0 (2026-01-16)
- 🧰 **Feature**: 新增「自定义导出」页面：选择数据源 → 获取数据 → 字段网格勾选 → 导出 CSV（支持全选/筛选字段/导出所选或全部字段）。
- 📦 **Feature**: 新增批量生成导出（一次最多 6 个），支持 `;`/`；` 分号分隔输入，自动去重并输出 ZIP（每只股票含增强版 OHLCV + 原始 Hist Data）。
- ⏳ **UX**: 批量生成增加加载状态（spinner + 当前处理项提示 + 进度条），避免点击后无反馈。
- 🧭 **UI**: 右侧悬浮导航新增「自定义导出」入口，便于在各页面快速跳转。

## v1.0.1 (2025-01-13)
- 📱 **UI**: 适配手机浏览器（支持手动开启手机模式）。

## v1.0.0 (2025-01-12)
- 🎉 **Initial Release**: 基础 A 股历史行情导出功能上线。
- ✨ **Feature**: 新增版本发布模块，方便追踪更新日志。
- ✨ **Feature**: 支持股票代码与名称双向搜索 (Code <-> Name)。
- 🛠 **Core**: 支持导出增强版 OHLCV 和原始历史数据。
- 📦 **Deployment**: 适配 Streamlit Community Cloud 部署。
